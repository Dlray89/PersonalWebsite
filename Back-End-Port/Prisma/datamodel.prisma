type Contact {
    id: ID! @id
    name: String!
    email: String!
    phoneNumber: String!
    comment: [Comment!]! @relation(name: "ContactByComment")
}

type Comment {
    id: ID! @id
    message: String!
    contacts: [Contact!]! @relation(name: "ContactByComment")
}